services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: cartera
      POSTGRES_USER: cartera_app
      POSTGRES_PASSWORD: cartera_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
volumes:
  postgres_dev_data:
